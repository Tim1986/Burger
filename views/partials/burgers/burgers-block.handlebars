<li>
    {{burger_name}}

    {{#unless devoured}}
    <button class="change-devoured" data-id="{{id}}" data-newdevoured="true">
        DEVOUR!
    </button>
    {{/unless}}

    {{#if devoured}}
    <button class="change-devoured" data-id="{{id}}" data-newdevoured="false">
        REGURGITATE!
    </button>
    {{/if}}
</li>